Le dossier config-repo est est à placer de tel sorte qu'il n'y ai pas d'espace dans le chemin
Exemple dans l'appliication.properties de config-server : spring.cloud.config.server.native.search-locations=file:D:/config-repo
Le profil étudiant s'obtient en utilisant le fichier docker compose.
